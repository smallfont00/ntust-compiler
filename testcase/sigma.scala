/* Sigma.scala
 *
 * Compute sum = 1 + 2 + ... + n
 */

 object Sigma


{
  // constants and variables
  val n = 10
  var sum: int
  var elsee: int

  def main () 
  {
    sum = 0
    elsee = 0
    
    if (sum > 0) 
    if (sum > 0)
    print(1) 
    else
    if (sum < 0)
    print(2)
    else print(3)

    while (elsee <= n) {
      sum = sum + elsee
      elsee = elsee + 1
    }
    print ("The sum is ")
    println (sum) 
    }
}
